import BombieriVinogradov.Definitions.ResearchMode

/-!
# Research-mode helper

This module supplies only the technical placeholder used before the exact
headline theorem is installed in `Challenge.lean`.
-/

namespace BombieriVinogradov

/-- The research placeholder is inhabited; this is build wiring, not mathematics. -/
theorem formalizationTargetPending : FormalizationTargetPending := by
  trivial

end BombieriVinogradov
